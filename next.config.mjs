/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',
    images: {
      unoptimized: true,
    },
    basePath: '/portfolio-site-next', // change this to your repo name
  };
  
  export default nextConfig;
  